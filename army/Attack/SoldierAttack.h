#pragma once

#include "IAttack.h"

class SoldierAttack : public IAttack
{
public:
    SoldierAttack();
    ~SoldierAttack();

    // void attacking(IUnit& unit, IUnit& enemy);
    // void counterAttacking(IUnit& unit, IUnit& enemy);
};